services:
  worker:
    build: ./worker
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - jobs_data:/jobs

volumes:
  jobs_data:
    name: gpu-job-scheduler-jobs